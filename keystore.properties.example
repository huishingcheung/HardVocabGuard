# 将此文件复制为 keystore.properties 并填写真实信息（不要提交 keystore.properties）
# storeFile 可填相对路径，例如：keystore/release.jks

storeFile=keystore/release.jks
storePassword=CHANGE_ME
keyAlias=CHANGE_ME
keyPassword=CHANGE_ME

